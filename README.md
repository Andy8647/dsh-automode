# dsh-automode → moved

The code now lives in **[Andy8647/dsh-auto-approval](https://github.com/Andy8647/dsh-auto-approval)**
(npm: [`dsh-auto-approval`](https://www.npmjs.com/package/dsh-auto-approval), v0.2.0+).

This repository was the working copy for the 0.2.0 rewrite; it is archived so the
plugin has one canonical home that matches its npm package name.
